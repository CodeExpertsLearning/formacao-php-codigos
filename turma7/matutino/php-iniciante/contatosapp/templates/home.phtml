
<a href="/criar">Criar Usuário</a>

<ul>
    <?php foreach($contacts->fetchAll(PDO::FETCH_ASSOC) as $c):?>
        <li>
            <?=$c['name'];?> - <?=date('d/m/Y H:i:s', strtotime($c['created_at']));?>
        </li>
    <?php endforeach;?>
</ul>